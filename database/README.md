# Database

PostgreSQL database name:

```sql
locker_system
```

The active Prisma schema lives in `backend/prisma/schema.prisma` so Prisma commands run from the backend package without creating root-level npm artifacts.
